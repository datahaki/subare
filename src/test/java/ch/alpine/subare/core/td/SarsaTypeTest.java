// code by jph
package ch.alpine.subare.core.td;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

class SarsaTypeTest {
  @Test
  void test() {
    assertEquals(SarsaType.values().length, 3);
  }
}
